# Spreadsheet Sources

Place local spreadsheet inputs referenced by `references.yaml` in this folder. Large source files are ignored by default and should not be committed unless a later work package explicitly introduces fixtures.
