# RFP Sources

Place local RFP PDFs referenced by `references.yaml` in this folder. Source PDFs are ignored by default and should not be committed unless a later work package explicitly introduces fixtures.
